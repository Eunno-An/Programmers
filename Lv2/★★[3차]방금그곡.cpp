#include <string>
#include <vector>

using namespace std;
//시간이 -일 때 주의하기
//단순히 find로 풀어도 시간초과 안남.
string solution(string m, vector<string> musicinfos) {
    string answer = "";
    
    return answer;
}
